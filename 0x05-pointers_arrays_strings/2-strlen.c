#include <stdio.h>
#include "main.h"
#include <string.h>

/**
 * _strlen - function to print string length
 * @len: variable to hold strlen
 */

int _strlen(char *s)
{
	int len;
	len = strlen(s);

	return (0);
}
