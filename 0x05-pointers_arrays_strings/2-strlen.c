#include <stdio.h>
#include "main.h"
#include <string.h>

/**
 * _strlen - function to print string length
 * @len: variable to hold strlen
 */

int _strlen(char *s)
{
	static int n;
	n = strlen(s);

	return (0);
}
