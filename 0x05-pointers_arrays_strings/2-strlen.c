#include <stdio.h>
#include "main.h"
#include <string.h>

/**
 * _strlen - function to print string length
 * @len: variable to hold strlen
 * Return: returns n
 */

int _strlen(char *s)
{
	int n;
	n = strlen(s);

	return (n);
}
