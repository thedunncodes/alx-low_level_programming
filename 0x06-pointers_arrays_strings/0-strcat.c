#include <stdio.h>
#include <string.h>
#include "main.h"

/**
 * main - so concatenate s2 to s1
 * @dest: s1 holder
 * @src: s2 holder
 * @cat: character variable
 * Return: Always 0.
 */

char *_strcat(char *dest, char *src)
{
	char cat;

	cat = strcat(dest, src);

	/*return (dest, src);*/
}
